// Placeholder — real dashboard UI (four metric cards) is a later session.
export function DashboardPage() {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold text-gray-900">Dashboard</h1>
    </main>
  );
}
